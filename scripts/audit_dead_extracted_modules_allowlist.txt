# audit_dead_extracted_modules_allowlist.txt — W1768 guard allowlist
#
# Формат записей (по одной на строку, "#" — комментарий):
#   module:<relpath>            — модуль целиком разрешён как «dead» (не флагается)
#   dup:<symbol>@<relpath>      — символ@извлечённый-модуль разрешён как «dead dup»
#
# Будь КОНСЕРВАТИВЕН. Сюда попадают ТОЛЬКО заведомо-намеренные случаи:
# pre-existing, покрытое тестами scaffolding, ещё не подключённое в живой
# production-путь, и НЕ являющееся decorative-extraction регрессией класса
# W746/W797. Записи ниже подтверждены: каждый модуль имеет dedicated-тесты и
# ноль production-импортёров по дизайну (будущее использование).
#
# НЕ добавляй сюда W797-находки (glossary_service.py и подобные decorative-
# extraction регрессии) — это и есть баг, который страж обязан ловить.
# W1769: ipc_dispatch.py и service_logging.py УДАЛЕНЫ (диспетчеризация и
# логирование консолидированы инлайн в service.py); IPCServer/configure_logging
# dead-dups устранены (#1601 + W1769) — их больше нет в дереве, поэтому в
# allowlist они не нужны и не должны попадать.

# --- Phase 3 (Call Automation) телефония: фабрика провайдера ещё не вызвана
#     из живого пути; интерфейс + тесты готовы (test_call_provider_factory.py).
module:KrabEar/backend/call_provider_factory.py

# --- Voice Assistant multimodal client — экспериментальный, под фичефлагом;
#     покрыт test_va_multimodal.py, не подключён в production-пайплайн.
module:KrabEar/backend/va_multimodal.py

# --- Phase 4 STT router factory — будущая точка сборки STTRouter; покрыта
#     test_stage_pipeline_stt_router_factory.py, ещё не в живом пути.
module:KrabEar/core/pipeline/stt_router_factory.py

# --- Общая retry-утилита (RetryConfig/RetryStrategy) — библиотечный helper,
#     покрыт test_retry_strategy.py; пока не вызывается из production-кода.
module:KrabEar/core/retry_strategy.py

# --- W1775: CallSilenceProbe — РЕАЛЬНЫЙ аудио-анализ тишины (RMS по PCM + TTS
#     probe), покрыт test_call_silence_probe.py. Раньше он импортировался в
#     service.py ТОЛЬКО ради декоративного аргумента CallAutoEnd(silence_probe=…),
#     который никогда не вызывался (assigned-and-never-read) — этот мёртвый импорт
#     удалён при honesty de-decoration. Сам класс — НЕ decorative-регрессия: это
#     намеренно сохранённая аудио-capability для будущего реального подтверждения
#     тишины (Swift-агент / audio-pipeline). Production-импортёров пока ноль по
#     дизайну, поэтому allowlist.
module:KrabEar/backend/call_silence_probe.py

# --- Memory Conductor T1 (2026-08-19): LedgerClient — write-only ledger
#     foundation module (spec docs/superpowers/specs/2026-08-19-memory-conductor-
#     delta, fail-closed GC, corrupt-recovery, конкурентная публикация двух
#     владельцев, sidecar-lock source-контракт, nowait-read). Production-
#     подключение (IPC-процесс публикует свои резиденты, REST — свои) — задача
#     последующей волны того же плана (T2+), намеренно НЕ в этой задаче.
