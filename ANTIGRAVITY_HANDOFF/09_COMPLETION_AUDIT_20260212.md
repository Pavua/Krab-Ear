# Antigravity Completion Audit (2026-02-12)

## Итог

1. Общая интеграционная приёмка (`Run Antigravity Acceptance.command`) проходит: build/tests/smoke зелёные.
2. Это подтверждает стабильность системы, но не подтверждает 100% закрытие всех task-item из handoff.

## Статус по прошлому пакету

### A. Krab Ear Native UI

1. `История` как рабочая вкладка:
- Статус: `СДЕЛАНО`.
- Признак: панель выбирает вкладку истории, есть сохранение `ui_last_tab`.

2. Responsive-правила (<1100/<900):
- Статус: `СДЕЛАНО`.
- Признак: `windowDidResize` переключает layout и компактные подписи.

3. Горячие клавиши пресетов:
- Статус: `СДЕЛАНО`.
- Признак: `Cmd+1/2/3` в `performKeyEquivalent`.

4. Визуальный индикатор активных фильтров (badge/label):
- Статус: `НЕ СДЕЛАНО`.
- Признак: в контроллере нет badge/label с числом активных фильтров.

5. Онбординг/быстрый старт с проверкой разрешений:
- Статус: `НЕ СДЕЛАНО`.
- Признак: в `main.swift` нет отдельного onboarding-экрана/потока.

### B. iOS клиент (Krab Voice Gateway/ios)

1. SwiftUI каркас + экран сессии + start/stop + partial subtitles placeholder:
- Статус: `СДЕЛАНО`.

2. Экран конфигурации Gateway URL/API key + health-check:
- Статус: `СДЕЛАНО`.

3. CallKit/PushKit wiring (каркас):
- Статус: `СДЕЛАНО (КАРКАС)`.
- Ограничение: пока без production VoIP интеграции.

### C. OpenClaw orchestration

1. Команды `!callstart`, `!callstop`, `!callstatus`, `!notify`, `!calllang`:
- Статус: `СДЕЛАНО`.

2. Риск дублирования обработчиков:
- Статус: `ТРЕБУЕТ ЧИСТКИ`.
- Признак: команды присутствуют одновременно в `src/handlers/tools.py` и `src/handlers/commands.py`, что может давать конфликт/двойные ответы.

## Вывод

1. Закрыто примерно `70-80%` предыдущего пакета.
2. Не закрыты/не доведены до DoD:
- Filter badge в истории;
- Онбординг быстрый старт;
- Дедупликация voice-команд в OpenClaw.
