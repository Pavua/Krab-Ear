# План волны S3: включение in-process REST у владельца

Спека: `docs/superpowers/specs/2026-07-31-s3-rest-flip-design.md` (v3, коммит `c0dd81b5`).
Базовый SHA: `c0dd81b5` на ветке `codex/krab-ear-v2`.
Ветка волны: `claude/s3-inprocess-enable`.

12 задач в 4 чанках. Порядок между чанками жёсткий. Внутри чанка 2 задача 6
обязана предшествовать задаче 7.

---

## Глобальные ограничения (действуют для КАЖДОЙ задачи)

Эти правила выдаются каждому воркеру дословно вместе с текстом его задачи.

**Границы.**
- Работать ТОЛЬКО в своём worktree, первым действием — `git checkout -b <ветка>`.
- В рабочем дереве основного чекаута есть чужая незакоммиченная работа
  (`KrabEar/backend/telegram_bridge.py`, `wake_word_models/hard_negatives_raw/`,
  два бинаря `KrabEarAgent`). Не трогать ни при каких обстоятельствах.
- Не пушить в общие ветки. Не мержить. Не деплоить.
- **Не запускать прод-бинарь и не рестартовать прод-backend.** Single-instance
  guard убьёт живой экземпляр владельца. Все проверки — на throwaway-инстансе с
  временным `--data-dir`.

**TDD.** Ни строки продакшн-кода без падающего теста. RED обязан падать по
правильной причине (фича отсутствует), а не из-за опечатки. Исключение — правки
докстрингов и шаблонов без поведения.

**Мок обязан отражать реальное поведение зависимости.** Зелёный тест поверх
выдуманного мока доказывает выдумку. Для путей с побочным эффектом (запись,
отправка) — тестировать через настоящий тап, no-op мок маскирует двойной и
пропущенный эффект.

**Гейты перед сдачей задачи** (воркер запускает сам и прикладывает вывод):
```bash
PYTHONPATH="$(pwd)/KrabEar" python -m pytest <свои тест-файлы> -p no:cacheprovider -q
```
- ubuntu-parity для всех изменённых тест-файлов: `scripts/pre_merge_py312_check.sh <файлы>`.
  🔴 Дев-venv — Python 3.14 **с** mlx; ubuntu-CI — 3.12 **без** mlx. Тест,
  утверждающий, что `import mlx_whisper` удался, зелёный локально и красный в CI.
- flake8 командой из CI (в тестах `W293` не расслаблен).
- `make audit-all` — обязателен для задач, создающих новый модуль.
- `swift build -c release` — для задач, трогающих `native/`.

**Тест с `BackendService`.** Любой тест, конструирующий `BackendService(...)`,
ОБЯЗАН звать `service.close()` в `tearDown`. Иначе daemon-треды на выходе
интерпретатора дают `exit(1)` и валят весь файл чанка.

**Тред-стабы.** Не наследовать `threading.Thread` с переопределённым `start()`
без `super().start()` — тред застревает в `threading._limbo`, и процесс виснет на
выходе. Писать чистый duck-type класс (образец —
`test_webhook_redirect_ssrf_W1355.py`).

**Логирование.** `logger.info("сообщение", extra={"ключ": значение})`. `print()` в
продакшн-коде запрещён.

**Язык.** Комментарии и докстринги — русские, как в окружающем коде. Комментарий
пишется только для ограничения, которое код не может показать сам.

**Отчёт.** Статус `DONE` / `DONE_WITH_CONCERNS` / `BLOCKED` / `NEEDS_CONTEXT`,
список коммитов, вывод гейтов, вопросы. При `BLOCKED` — не повторять попытку
вслепую.

---

## Чанк 1 — данные, локи, приватность

Все четыре задачи независимы между собой и могут исполняться параллельно, кроме
живой верификации задачи 3, которая требует деплоя задачи 1.

### Задача 1 — выравнивание каталога данных и точка входа

**Спека:** Р3 + Р9 + находки I2, I-D, M1, M3.

**Почему.** Каталог попадает в процесс двумя разнотипными каналами: аргумент
`--data-dir` доходит только до `StateStore` через локальную переменную `main()`,
а всё, что читает `settings.DATA_DIR`, смотрит на переменную окружения. Сегодня
оба случайно указывают в один каталог, потому что переменная есть в rest-плисте.
После включения режима REST переезжает под backend-плист и теряет её.

**Что сделать.**

1. `KrabEar/launchagents/ai.krab.ear.backend.plist.template` — в
   `EnvironmentVariables`, рядом с `PYTHONPATH`:

```xml
    <!-- S3/Р3: settings.DATA_DIR обязан совпадать с --data-dir выше.
         Backend передаёт каталог АРГУМЕНТОМ (он доходит только до StateStore),
         а всё, что читает settings.DATA_DIR — rest_server, cloud_stt,
         cloud_rewriter, startup_diagnostics — смотрит сюда. Пока REST жил
         отдельным юнитом, переменная была в его плисте (с 16-07); при слиянии
         процессов она обязана быть здесь, иначе половина процесса читает
         ~/.krab_ear_data, а история пишется в канонический каталог.
         Значение обязано совпадать с --data-dir буквально — это проверяет
         test_backend_plist_data_dir_parity_S3.py. -->
    <key>KRAB_EAR_DATA_DIR</key>
    <string>__HOME__/Library/Application Support/KrabEar</string>
```

2. В том же файле, в `ProgramArguments`, заменить

```xml
    <string>__PROJECT_ROOT__/KrabEar/backend/service.py</string>
```

на

```xml
    <string>__PROJECT_ROOT__/KrabEar/main.py</string>
```

   Причина в комментарии над массивом:

```xml
  <!-- S3/Р9: запускаем main.py, а НЕ backend/service.py напрямую. Прямой запуск
       делает service.py модулем __main__, а rest_server.py:37 импортирует
       backend.service — и при включённом in-process REST файл исполняется
       ВТОРЫМ экземпляром модуля: в процессе живут два разных класса
       BackendService. Сегодня безвредно, но любой module-level синглтон,
       добавленный в service.py, тихо раздвоится. -->
```

3. Новый тест `KrabEar/tests/test_backend_plist_data_dir_parity_S3.py`:

   - парсит шаблон через `plistlib`;
   - достаёт значение, идущее в `ProgramArguments` сразу после `--data-dir`;
   - достаёт `EnvironmentVariables["KRAB_EAR_DATA_DIR"]`;
   - утверждает побайтовое равенство;
   - отдельным тестом утверждает, что `ProgramArguments` содержит `KrabEar/main.py`
     и **не** содержит `KrabEar/backend/service.py`.

   🔴 Шаблон содержит нераскрытые плейсхолдеры `__HOME__` и `__PROJECT_ROOT__` —
   тест сравнивает строки как есть, ничего не подставляя.

4. Busy-гейт перед переустановкой юнита. `scripts/install_backend_launchagent.command`
   делает **безусловный** `bootout` (строки 60-64) — под диктовкой это потеря
   аудио, инцидент 22 июля. Добавить в начало скрипта проверку по образцу
   `scripts/safe_backend_restart.command` (функция `busy_reason()`, строки 55-70):
   опросить `get_recording_state` и `get_meeting_live_state` через IPC, при
   занятости отказаться с внятным сообщением и кодом возврата 1; поддержать
   `--wait N` (ждать освобождения) и `--force` (осознанный обход).

   Если сокета нет — установка идёт как раньше (первичная установка, backend ещё
   не запускался): отсутствие сокета не является занятостью.

   Источник правды по exit-кодам — `safe_backend_restart.command`: 0 — успех,
   1 — отказ или сбой, 2 — ошибка использования.

5. Журнал ожидаемого поведения. В `docs/superpowers/plans/` создать
   `2026-07-31-s3-datadir-consumers.md` — таблица из семи потребителей
   `settings.DATA_DIR` (список в спеке Р3), для каждого: что читал до, что будет
   читать после, ожидаемый эффект. Две строки требуют явного вывода:
   `cloud_stt.py:25` и `cloud_rewriter.py:141` начнут читать канонический
   `settings.json` вместо несуществующего файла. Приватностный гейт
   `engine._cloud_rewrite_allowed()` не трогается — зафиксировать это письменно.

**Чего НЕ делать.** Не переписывать `sed` в установщике: он уже делает
единственную подстановку `__HOME__`, обе строки выведутся из неё. Не
переустанавливать живой юнит — это шаг деплоя, не задачи.

**Тесты:** `test_backend_plist_data_dir_parity_S3.py` (новый), плюс проверить, что
`test_migration_scripts.py` не покраснел от правки установщика.

**Определение готовности:** тест падает на текущем шаблоне (RED — переменной нет),
зеленеет после правки; busy-гейт отказывается при засимулированном
`is_recording: true`.

---

### Задача 2 — устранение лок-мины в облачных модулях

**Спека:** Р3, находка I-C. 🔴 Самая тонкая задача чанка.

**Почему.** `cloud_stt.py:25` и `cloud_rewriter.py:141` строят собственные
`StateStore(settings.DATA_DIR)`. Сегодня они смотрят в несуществующий
`~/.krab_ear_data` и потому безвредны. Задача 1 сводит их на **те же файлы**, что
основной store процесса.

Фикс дедлока #1872 — per-thread depth-counter — живёт в **поле экземпляра**
(`state_store.py:111`: `self._lock_depth: dict[int, int]`). Между экземплярами он
не работает: тред, держащий лок экземпляра А и вошедший в лок экземпляра Б, не
увидит `depth > 0`, возьмёт `fcntl.flock` на новом файловом дескрипторе и
самозаклинится навсегда — вместе со всем `StateStore` (34 call-site).

Прямого пути сегодня нет, но мину взводит задача 1.

**Что сделать.**

Оба модуля используют store **только** для `load_settings()` — проверено:
`cloud_stt.py:128, 215, 275` и `cloud_rewriter.py:171, 227, 290, 367, 399`. Значит
им не нужен `StateStore` вообще.

1. В обоих модулях заменить module-level `store = StateStore(settings.DATA_DIR)`
   на инъецируемый аксессор настроек — паттерн, уже принятый в проекте
   (`privacy_mode_fn`, `settings_fn` у `TranscriptVersionManager`,
   `CollectionManager` и других):

```python
# S3/I-C: свой StateStore здесь был миной. После выравнивания DATA_DIR (задача 1)
# он смотрел бы на ТЕ ЖЕ файлы, что основной store процесса, а per-thread
# depth-counter реентерабельности (#1872) живёт в поле ЭКЗЕМПЛЯРА — между двумя
# экземплярами он не защищает: вход в лок второго из-под лока первого берёт
# flock на новом fd и заклинивает навсегда. Читаем настройки через аксессор
# владельца процесса; фоллбэк на собственный ленивый store оставлен для
# standalone-режима и тестов, где владельца нет.
_settings_fn: Callable[[], dict] | None = None


def adopt_settings_reader(settings_fn: Callable[[], dict]) -> None:
    """Подменяет источник настроек ссылкой на аксессор владельца процесса."""
    global _settings_fn
    _settings_fn = settings_fn


def _load_settings() -> dict:
    if _settings_fn is not None:
        return _settings_fn()
    return _fallback_store().load_settings()
```

   `_fallback_store()` — ленивый синглтон, создающий `StateStore(settings.DATA_DIR)`
   при первом обращении. Ленивость важна: она убирает и транзитный дубль на
   импорте.

2. Все восемь мест `store.load_settings()` заменить на `_load_settings()`.

3. В `service.py`, в том же блоке, где вызывается `adopt_external_singletons`,
   подключить оба модуля к `self._cached_settings`. Вызов обязан идти
   **безусловно**, а не только при включённом in-process REST: `cloud_rewriter`
   зовётся из `engine.py` и живёт в backend-процессе уже сегодня.

4. Инвариант-тест `KrabEar/tests/test_state_store_cross_instance_lock_S3.py`:

   - два экземпляра `StateStore` на один временный каталог;
   - тред берёт лок первого и пытается войти в лок второго **с таймаутом**;
   - утверждает, что при текущей реализации это заклинивает (документирует
     класс), и что продакшн-путь после фикса такого вложения не создаёт.

   🔴 Тест обязан использовать таймаут и не может полагаться на то, что
   заклинивание разрешится. Реализовать через отдельный тред и `join(timeout=...)`,
   проверяя `is_alive()`. Тред-стаб не наследовать от `threading.Thread`.

5. Отдельный тест: после `adopt_settings_reader` ни один вызов не создаёт
   `StateStore` — замокать конструктор и утвердить ноль вызовов.

**Чего НЕ делать.** Не менять `state_store.py`. Не делать `_lock_depth` полем
класса — это выглядит соблазнительно, но сломает изоляцию экземпляров в тестах и
не решает случай двух процессов.

**Определение готовности:** инвариант-тест краснеет на текущем коде, зеленеет
после фикса; `cloud_rewriter` и `cloud_stt` читают настройки владельца.

---

### Задача 3 — `temp_uploads` в privacy-purge

**Спека:** Р11 + находка M-B.

**Почему.** `TEMP_DIR = settings.DATA_DIR / "temp_uploads"` (`rest_server.py:783`)
не упоминается нигде в коде очистки — единственное вхождение во всём backend.
Каталог содержит сырое пользовательское аудио из `POST /v1/stt/transcribe`.
Слияние делает backend его владельцем, а `handle_purge_all_data` его не трогает.
`scripts/audit_purge_coverage.py --fail-on-found` — включённый CI-гейт.

**Что сделать.**

1. В `handle_purge_all_data` добавить шаг очистки `temp_uploads`: удалять
   содержимое каталога, сам каталог сохранять (его ждёт `TEMP_DIR.mkdir` на
   импорте). Ошибки отдельных файлов — не прерывать очистку, логировать.
2. Тест: засеять каталог файлами, вызвать purge, утвердить, что файлов нет, а
   каталог существует.
3. Прогнать `python scripts/audit_purge_coverage.py --fail-on-found` — гейт должен
   пройти без внесения записи в allowlist.

**Чего НЕ делать.** Не вносить `temp_uploads` в
`scripts/purge_coverage_allowlist.txt`: его политика требует туда только то, что
демонстрируемо не является пользовательским содержимым.

**🔴 Живая верификация — отдельным шагом после деплоя задачи 1.** Обвязка
вычисляет путь от `settings.DATA_DIR`. Пока юнит не переустановлен, процесс видит
`~/.krab_ear_data` → purge чистит пустой каталог, а настоящий `temp_uploads`
остаётся с аудио. Статический аудит этого не поймает. В отчёте задачи явно
записать, что живая проверка отложена до деплоя, и что именно надо проверить.

---

### Задача 4 — честная диагностика при сбое сборки

**Спека:** Р5 + находки I1, M-A.

**Почему.** Внешний `except` (`service.py:828-831`) ставит
`self._rest_inprocess = None`, а `_get_rest_inprocess_summary` на `None` отдаёт
словарь, байт в байт совпадающий с «рубильник выключен». ErrorBus не пушится.
Для канарейки это ложноотрицательный сигнал на две недели.

Дыра уже, чем кажется: `start()` документирован как «НИКОГДА не бросает» и
возвращает `bool` во всех ветках, значит внешний `except` ловит только сбой
сборки приложения — импорт, `adopt_external_singletons`, `create_app()`,
конструктор.

**Что сделать.**

1. `InProcessRestServer.__init__` принимает `enabled: bool` параметром и
   **перестаёт читать** `settings.REST_IN_PROCESS_ENABLED` сам
   (`rest_inprocess.py:38`). Иначе рубильник двухголовый: владелец включает
   настройкой, `service.py` конструирует сервер, а тот молча возвращает `False`.

2. В `service.py` при сбое сборки не обнулять поле, а ставить надгробие:

```python
class _RestInProcessTombstone:
    """Надгробие: сборка REST-приложения упала, лечить нечего.

    Существует, чтобы диагностика отличала «рубильник выключен» от «включён,
    но не поднялся»: без него обе ситуации давали идентичный ответ, и канарейка
    две недели видела бы штатную картину при мёртвом REST.
    """

    def __init__(self, *, enabled: bool, port: int, error: str) -> None:
        self._status = {
            "enabled": enabled,
            "running": False,
            "port": port,
            "error": error,
            "tombstone": True,
        }

    def status(self) -> dict:
        return dict(self._status)

    def stop(self, timeout: float | None = None) -> None:
        """Останавливать нечего — метод есть ради единообразия с close()."""
```

   Поле `tombstone` — не косметика: сторож задачи 7 обязан по нему отличать
   нелечимое, иначе будет вечно лечить надгробие и выдаст `rest.wedged` поверх
   уже отправленного `rest.startup_failed`.

3. Новый код ошибки `rest.startup_failed` в `error_codes.py` по образцу
   `rest.port_conflict` (строки 842-853): severity `error` (в отличие от `warn` у
   конфликта порта — там fail-open с живым легаси-REST, здесь REST не работает
   вообще), `actionable: False`, `dedupe_seconds: 300`. Пуш — из ветки `except`
   через существующий `_push_rest_error`.

4. Тесты: три состояния диагностики различимы (выключено / работает / сборка
   упала); при сбое сборки ErrorBus получает `rest.startup_failed`; `enabled`
   в `status()` совпадает с переданным параметром, а не с `settings`.

**Чего НЕ делать.** Не менять `health_check_service.py` — он уже умеет звать
`status()` у коллаборатора, и в этом смысл надгробия. Не конструировать
настоящий `InProcessRestServer` при выключенном рубильнике: `create_app()` тянет
импорт `rest_server` со вторым `AudioEngine`.

---

## Чанки 2-4

Детализируются отдельным коммитом после того, как чанк 1 пройдёт ревью — их
формулировки зависят от того, как лягут интерфейсы задач 2 и 4 (аксессор
настроек и параметр `enabled` используются дальше).

Состав зафиксирован спекой §5 и не меняется:

**Чанк 2 — жизненный цикл.** 5) дренаж с флагом `shutting_down` и посчитанным
бюджетом; 6) динамическое подавление моста; 7) сторож REST. Порядок 6 → 7
обязателен.

**Чанк 3 — клиенты, остатки, замеры.** 8) переподключение SSE в скретчпаде;
9) остатки; 10) смок и три сценария замеров.

**Чанк 4 — включение у владельца.** 11) migrate с busy-гейтом, `bootout` без
удаления plist и `--rollback`; 12) включение, проверка обоих путей отката, старт
канарейки, бриф в сессию Voice Gateway.
