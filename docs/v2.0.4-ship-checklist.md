# v2.0.4 Ship Checklist

**Target ship date:** TBD by user
**Previous version:** v2.0.3 (2026-05-20, UUID FDAB353F-...)

## What's new in v2.0.4

### Bug fixes
- 🔴 GigaAM padding errors on 24-30s clips (Wave 359 PR #595): 2 bugs — longform threshold + chunker micro-advance
- 🔧 Sentry dist tracking (Wave 326 PR #588): 3 bugs fixed (dist not set, release=nil, no release page)
- 🔒 Path traversal security (Wave 342 PR #592): relative paths now rejected
- 🔧 LM Studio Stream(gpu) chronic (Wave 306 PR #584): reclassified + 2s retry

### Refactor
- 🧹 RecordingCoreService extracted (Wave 331 PR #589): -833 LOC service.py
- 🧹 5-phase _handle_stop_recording orchestrator (Wave 88, shipped в v2.0.3)
- 🧹 6 services extracted total

### Test coverage
- 200+ new tests since v2.0.3
- Production code: 0 flake8 warnings
- Sentry: 0 unresolved agent

## Pre-ship verification steps

1. [ ] All Wave 274+ PRs merged
2. [ ] CLAUDE.md handler count synced (check 296 or current)
3. [ ] ERROR_REGISTRY count verified (48+ codes)
4. [ ] Run swift build clean
5. [ ] Bundle binary tested locally (manual smoke recording)
6. [ ] Backup current settings.json
7. [ ] Verify launchd plist working

## Ship procedure (same as v2.0.3)

```bash
cd /Users/pablito/Antigravity_AGENTS/Krab\ Ear
plutil -replace CFBundleShortVersionString -string "2.0.4" "Krab Ear.app/Contents/Info.plist"
plutil -replace CFBundleVersion -string "2.0.4" "Krab Ear.app/Contents/Info.plist"
cd native/KrabEarAgent && swift build -c release
cp -f .build/release/KrabEarAgent "../../Krab Ear.app/Contents/MacOS/KrabEarAgent"
cp -f .build/release/KrabEarAgent ../../native/runtime/KrabEarAgent
codesign -s "Krab Ear Dev Local" -f --options=runtime "../../Krab Ear.app/Contents/MacOS/KrabEarAgent"
codesign -s "Krab Ear Dev Local" -f --options=runtime ../../native/runtime/KrabEarAgent

# Verify
dwarfdump --uuid "../../Krab Ear.app/Contents/MacOS/KrabEarAgent"

# Sentry release create + dSYM upload
SENTRY_AUTH_TOKEN=$(grep SENTRY_AUTH_TOKEN /Users/pablito/Antigravity_AGENTS/Краб/.env | cut -d= -f2 | tr -d '"' | tr -d "'")
sentry-cli --auth-token "$SENTRY_AUTH_TOKEN" --url https://de.sentry.io releases new -p krab-ear-agent v2.0.4
sentry-cli --auth-token "$SENTRY_AUTH_TOKEN" --url https://de.sentry.io upload-dif --org po-zm --project krab-ear-agent .build/release/KrabEarAgent.dSYM
sentry-cli --auth-token "$SENTRY_AUTH_TOKEN" --url https://de.sentry.io releases finalize v2.0.4

# Git
cd ../..
git add "Krab Ear.app/Contents/Info.plist" "Krab Ear.app/Contents/MacOS/KrabEarAgent" "native/runtime/KrabEarAgent"
git commit -m "chore(wave364): v2.0.4 ship"
git tag v2.0.4
git push origin codex/krab-ear-v2 v2.0.4
```

## Post-ship verification

1. User Quit + relaunch Krab Ear.app
2. Verify About → version 2.0.4
3. Monitor Sentry 48h:
   - dist:2.0.4 events appearing
   - AGENT-J 0 events (SF Symbol fix holds)
   - AGENT-M 0 events (BackendToast prewarm holds)
   - No new regressions

## Known issues NOT fixed in v2.0.4
- pyannote VAD model HF gating — user manual accept needed (independent of Krab Ear code)
- VPN plist `KeepAlive=false` — separate fix (Wave 316)
- macOS auto-update reboot — separate fix (Wave 316)
