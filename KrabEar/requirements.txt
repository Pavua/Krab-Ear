mlx-whisper
pyannote.audio==4.0.4  # pinned: MPS working, Tahoe 26.5 compat verified 2026-04-18 (AF audit)
numpy
sounddevice
requests
pydantic-settings
soundfile
pyperclip
flask
flask-smorest
flask-sock>=0.7.0
marshmallow
pyobjc-framework-Cocoa; sys_platform == "darwin"
pyobjc-framework-Vision; sys_platform == "darwin"
websockets>=12.0
gunicorn>=21.2.0
flask-limiter>=3.5.0
flask-cors>=4.0.0

# --- Optional: GigaAM-RNNT v2 (RU-специализированная STT, MIT license) ---
# Официальный PyPI-пакет от salute-developers (Sber).
# Установи вручную если хочешь включить GigaAM для русского языка:
#   pip install gigaam
# После установки включи в настройках: KRAB_EAR_STT_GIGAAM_ENABLED=true
# Дополнительно для long-form аудио (>30 сек):
#   pip install gigaam[longform]  (требует HF_TOKEN для pyannote VAD)
# gigaam

# --- Optional: openWakeWord (free wake word detection, Apache 2.0) ---
# Установи вручную если нужен wake word без Picovoice:
#   pip install openwakeword
# openwakeword

# --- Optional: sentence-transformers для семантического поиска (opt-in) ---
# Требуется если KRAB_EAR_SEMANTIC_SEARCH_ENABLED=true.
# Рекомендуемые мультиязычные модели (RU/ES/EN):
#   intfloat/multilingual-e5-base   (~560 MB, по умолчанию)
#   mixedbread-ai/mxbai-embed-large-v1  (~670 MB)
# Установи вручную: pip install sentence-transformers
# После установки включи: KRAB_EAR_SEMANTIC_SEARCH_ENABLED=true
# sentence-transformers>=3.0

# --- Sentry SDK for crash/error telemetry (no-op без DSN в settings) ---
sentry-sdk>=2.0

# --- Test parallelization (CI speedup via -n auto) ---
pytest-xdist>=3.5
